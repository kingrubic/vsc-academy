{
  "name": "vsc-academy-cms",
  "private": true,
  "version": "1.0.0",
  "description": "VSC Academy Admin CMS — single source of truth for the static public site",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node lib/seed.js",
    "seed:force": "node lib/seed.js --force"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.8.1",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "1.4.5-lts.1"
  }
}
